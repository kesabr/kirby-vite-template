title: Home

icon: home

columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
          text:
            extends: fields/writer

  sidebar:
    width: 1/3
    sections:
      files:
        type: files
